const translations = {
  en: {
    'nav.home': 'Home',
    'nav.about': 'About',
    'nav.help': 'Help Center',
    'nav.contact': 'Contact',
    'hero.eyebrow': 'Premium Portfolio • Digital Creator',
    'hero.title': 'I craft beautiful digital experiences with purpose and precision.',
    'hero.text': 'I am Nayem Mia, a designer and developer focused on premium web experiences, thoughtful branding, and user-centered products.',
    'hero.contact': 'Let’s Connect',
    'hero.resume': 'Download Resume',
    'hero.visitors': 'Visitors',
    'hero.clock': 'Live Time',
    'about.eyebrow': 'About Me',
    'about.title': 'Blending aesthetics, strategy, and development.',
    'about.storyTitle': 'My Story',
    'about.storyText': 'I build modern websites that feel premium, fast, and intuitive on every screen. My focus is turning bold ideas into smooth digital experiences.',
    'about.infoTitle': 'Personal Info',
    'about.name': 'Name',
    'about.role': 'Role',
    'about.location': 'Location',
    'about.skillsTitle': 'Core Skills',
    'about.timelineTitle': 'Experience Timeline',
    'about.timelineOne': 'Started crafting interactive design systems.',
    'about.timelineTwo': 'Built premium web experiences for modern brands.',
    'help.eyebrow': 'Help Center',
    'help.title': 'Premium support application form.',
    'help.cardTitle': 'Need expert support?',
    'help.cardText': 'Share your requirements and I will respond with a tailored solution for your project.',
    'help.listOne': 'Fast response',
    'help.listTwo': 'Custom strategy',
    'help.listThree': 'Mobile-first execution',
    'help.name': 'Full Name',
    'help.email': 'Email',
    'help.phone': 'Phone Number',
    'help.subject': 'Subject',
    'help.message': 'Message',
    'help.attachment': 'Attachment Upload',
    'help.submit': 'Send Request',
    'contact.eyebrow': 'Contact',
    'contact.title': 'Let’s build something memorable together.',
    'contact.quickTitle': 'Quick Contact',
    'contact.mapTitle': 'Location',
    'contact.mapText': 'Based in Bangladesh and available for remote collaborations worldwide.',
    'notif.title': 'Notifications',
    'notif.one': 'New portfolio experience is live.',
    'notif.two': 'Dark mode and language switches are ready.',
    'notif.three': 'PWA support is enabled for installable browsing.',
  },
  bn: {
    'nav.home': 'হোম',
    'nav.about': 'আমার সম্পর্কে',
    'nav.help': 'সাহায্য কেন্দ্র',
    'nav.contact': 'যোগাযোগ',
    'hero.eyebrow': 'প্রিমিয়াম পোর্টফোলিও • ডিজিটাল ক্রিয়েটর',
    'hero.title': 'আমি উদ্দেশ্য ও নির্ভুলতার সাথে সুন্দর ডিজিটাল অভিজ্ঞতা তৈরি করি।',
    'hero.text': 'আমি নাঈম মিয়া, একজন ডিজাইনার ও ডেভেলপার, যিনি প্রিমিয়াম ওয়েব অভিজ্ঞতা, চিন্তাশীল ব্র্যান্ডিং এবং ব্যবহারকারী-কেন্দ্রিক পণ্য তৈরি করি।',
    'hero.contact': 'যোগাযোগ করুন',
    'hero.resume': 'রিজিউমি ডাউনলোড',
    'hero.visitors': 'পরিদর্শক',
    'hero.clock': 'সরাসরি সময়',
    'about.eyebrow': 'আমার সম্পর্কে',
    'about.title': 'সৌন্দর্য, কৌশল এবং ডেভেলপমেন্ট একত্রে।',
    'about.storyTitle': 'আমার গল্প',
    'about.storyText': 'আমি আধুনিক ওয়েবসাইট তৈরি করি যা প্রিমিয়াম, দ্রুত এবং প্রতিটি স্ক্রিনে স্বজ্ঞাত অনুভব করে।',
    'about.infoTitle': 'ব্যক্তিগত তথ্য',
    'about.name': 'নাম',
    'about.role': 'পেশা',
    'about.location': 'অবস্থান',
    'about.skillsTitle': 'মূল দক্ষতা',
    'about.timelineTitle': 'অভিজ্ঞতার সময়রেখা',
    'about.timelineOne': 'ইন্টার‍্যাকটিভ ডিজাইন সিস্টেম তৈরির শুরু করেছিলাম।',
    'about.timelineTwo': 'আধুনিক ব্র্যান্ডের জন্য প্রিমিয়াম ওয়েব অভিজ্ঞতা তৈরি করেছি।',
    'help.eyebrow': 'সাহায্য কেন্দ্র',
    'help.title': 'প্রিমিয়াম সাপোর্ট আবেদন ফর্ম।',
    'help.cardTitle': 'বিশেষজ্ঞ সহায়তা প্রয়োজন?',
    'help.cardText': 'আপনার প্রয়োজন শেয়ার করুন, আমি আপনার প্রকল্পের জন্য উপযুক্ত সমাধান দেব।',
    'help.listOne': 'দ্রুত প্রতিক্রিয়া',
    'help.listTwo': 'কাস্টম কৌশল',
    'help.listThree': 'মোবাইল-ফার্স্ট বাস্তবায়ন',
    'help.name': 'পূর্ণ নাম',
    'help.email': 'ইমেইল',
    'help.phone': 'ফোন নম্বর',
    'help.subject': 'বিষয়',
    'help.message': 'বার্তা',
    'help.attachment': 'সংযুক্তি',
    'help.submit': 'অনুরোধ পাঠান',
    'contact.eyebrow': 'যোগাযোগ',
    'contact.title': 'চলুন একসাথে কিছু স্মরণীয় তৈরি করি।',
    'contact.quickTitle': 'দ্রুত যোগাযোগ',
    'contact.mapTitle': 'অবস্থান',
    'contact.mapText': 'বাংলাদেশে অবস্থান করে বিশ্বব্যাপী রিমোট সহযোগিতার জন্য উপলব্ধ।',
    'notif.title': 'নোটিফিকেশন',
    'notif.one': 'নতুন পোর্টফোলিও অভিজ্ঞতা চালু হয়েছে।',
    'notif.two': 'ডার্ক মোড ও ভাষা পরিবর্তন প্রস্তুত।',
    'notif.three': 'PWA সমর্থন ইনস্টলযোগ্য ব্রাউজিংয়ের জন্য প্রস্তুত।',
  },
  tr: {
    'nav.home': 'Ana Sayfa',
    'nav.about': 'Hakkımda',
    'nav.help': 'Yardım Merkezi',
    'nav.contact': 'İletişim',
    'hero.eyebrow': 'Premium Portföy • Dijital Yaratıcı',
    'hero.title': 'Amaçlı ve titiz bir şekilde güzel dijital deneyimler yaratıyorum.',
    'hero.text': 'Ben Nayem Mia; premium web deneyimleri, düşünceli markalaşma ve kullanıcı odaklı ürünlere odaklanan bir tasarımcı ve geliştiriciyim.',
    'hero.contact': 'İletişime Geç',
    'hero.resume': 'Özgeçmiş İndir',
    'hero.visitors': 'Ziyaretçi',
    'hero.clock': 'Canlı Saat',
    'about.eyebrow': 'Hakkımda',
    'about.title': 'Estetik, strateji ve geliştirmeyi birleştiriyorum.',
    'about.storyTitle': 'Hikâyem',
    'about.storyText': 'Her ekranda premium, hızlı ve sezgisel hisseden modern web siteleri kuruyorum.',
    'about.infoTitle': 'Kişisel Bilgiler',
    'about.name': 'İsim',
    'about.role': 'Rol',
    'about.location': 'Konum',
    'about.skillsTitle': 'Temel Beceriler',
    'about.timelineTitle': 'Deneyim Zaman Çizelgesi',
    'about.timelineOne': 'Etkileşimli tasarım sistemleri oluşturmaya başladım.',
    'about.timelineTwo': 'Modern markalar için premium web deneyimleri yarattım.',
    'help.eyebrow': 'Yardım Merkezi',
    'help.title': 'Premium destek başvuru formu.',
    'help.cardTitle': 'Uzman desteğe mi ihtiyacınız var?',
    'help.cardText': 'İhtiyaçlarınızı paylaşın; projeniz için özel bir çözüm sunayım.',
    'help.listOne': 'Hızlı yanıt',
    'help.listTwo': 'Özel strateji',
    'help.listThree': 'Mobil öncelikli uygulama',
    'help.name': 'Tam Ad',
    'help.email': 'E-posta',
    'help.phone': 'Telefon Numarası',
    'help.subject': 'Konu',
    'help.message': 'Mesaj',
    'help.attachment': 'Ek Yükleme',
    'help.submit': 'İstek Gönder',
    'contact.eyebrow': 'İletişim',
    'contact.title': 'Birlikte unutulmaz bir şey inşa edelim.',
    'contact.quickTitle': 'Hızlı İletişim',
    'contact.mapTitle': 'Konum',
    'contact.mapText': 'Bangladeş merkezli ve dünya çapında uzaktan iş birlikleri için hazır.',
    'notif.title': 'Bildirimler',
    'notif.one': 'Yeni portföy deneyimi yayında.',
    'notif.two': 'Koyu mod ve dil değiştiriciler hazır.',
    'notif.three': 'PWA desteği kurulum dostu tarama için hazır.',
  },
};

const languageToggle = document.getElementById('languageToggle');
const themeToggle = document.getElementById('themeToggle');
const notificationBell = document.getElementById('notificationBell');
const notifPanel = document.getElementById('notifPanel');
const helpForm = document.getElementById('helpForm');
const helpStatus = document.getElementById('helpStatus');
const adminForm = document.getElementById('adminForm');
const adminStatus = document.getElementById('adminStatus');
const adminNavLink = document.getElementById('adminNavLink');
const adminProfileImageInput = document.getElementById('adminProfileImage');
const adminProfileFileInput = document.getElementById('adminProfileImageFile');
const adminAccessPanel = document.getElementById('adminAccessPanel');
const adminFormWrapper = document.getElementById('adminFormWrapper');
const adminPasswordInput = document.getElementById('adminPasswordInput');
const adminUnlockBtn = document.getElementById('adminUnlockBtn');
const adminLockStatus = document.getElementById('adminLockStatus');
const liveClock = document.getElementById('liveClock');
const year = document.getElementById('year');

let currentLang = localStorage.getItem('portfolio-lang') || 'en';
let currentTheme = localStorage.getItem('portfolio-theme') || 'dark';
let pendingProfileImage = null;

const defaultProfile = {
  displayName: 'Nayem Mia',
  heroTitle: 'I craft beautiful digital experiences with purpose and precision.',
  heroText: 'I am Nayem Mia, a designer and developer focused on premium web experiences, thoughtful branding, and user-centered products.',
  storyText: 'I build modern websites that feel premium, fast, and intuitive on every screen. My focus is turning bold ideas into smooth digital experiences.',
  role: 'UI/UX & Frontend Developer',
  location: 'Bangladesh',
  profileImage: 'assets/profile.svg',
  email: 'hello@nayem.dev',
  phone: '+880 1712 000000',
  facebook: '/nayem.mia',
  linkedin: 'https://www.linkedin.com',
  github: 'https://github.com',
  instagram: 'https://www.instagram.com',
};

function setLanguage(lang) {
  currentLang = lang;
  document.documentElement.lang = lang;
  document.querySelectorAll('[data-i18n]').forEach((node) => {
    const key = node.getAttribute('data-i18n');
    const value = translations[lang][key];
    if (value) {
      node.textContent = value;
    }
  });
  document.querySelectorAll('[data-i18n-placeholder]').forEach((node) => {
    const key = node.getAttribute('data-i18n-placeholder');
    const value = translations[lang][key];
    if (value) {
      node.setAttribute('placeholder', value);
    }
  });
  localStorage.setItem('portfolio-lang', lang);
  languageToggle.textContent = lang.toUpperCase();
}

function setTheme(theme) {
  currentTheme = theme;
  document.documentElement.setAttribute('data-theme', theme);
  themeToggle.textContent = theme === 'dark' ? '☀️' : '🌙';
  localStorage.setItem('portfolio-theme', theme);
}

function applyProfile(profile) {
  document.getElementById('brandName').textContent = profile.displayName;
  document.getElementById('heroTitle').textContent = profile.heroTitle;
  document.getElementById('heroText').textContent = profile.heroText;
  document.getElementById('storyText').textContent = profile.storyText;
  document.getElementById('profileName').textContent = profile.displayName;
  document.getElementById('profileRole').textContent = profile.role;
  document.getElementById('profileLocation').textContent = profile.location;
  document.getElementById('contactEmail').textContent = profile.email;
  document.getElementById('contactPhone').textContent = profile.phone;
  document.getElementById('contactFacebook').textContent = profile.facebook;
  document.getElementById('footerName').textContent = profile.displayName;
  document.getElementById('heroImage').src = profile.profileImage || 'assets/profile.svg';
  document.getElementById('heroImage').alt = `${profile.displayName} portrait`;
  document.getElementById('linkedinLink').href = profile.linkedin;
  document.getElementById('githubLink').href = profile.github;
  document.getElementById('instagramLink').href = profile.instagram;
  document.title = `${profile.displayName} | Premium Portfolio`;
}

async function loadProfileFromServer() {
  try {
    const response = await fetch('/api/profile');
    const data = await response.json();
    if (data.profile) {
      return data.profile;
    }
  } catch (error) {
    console.warn('Could not load profile from server, using fallback data.', error);
  }
  return defaultProfile;
}

async function saveProfileToServer(profile) {
  const response = await fetch('/api/profile', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(profile),
  });
  const result = await response.json();
  if (!result.ok) {
    throw new Error('Failed to save profile');
  }
  return result.profile;
}

function populateAdminForm(profile) {
  document.getElementById('adminDisplayName').value = profile.displayName;
  document.getElementById('adminHeroTitle').value = profile.heroTitle;
  document.getElementById('adminHeroText').value = profile.heroText;
  document.getElementById('adminStoryText').value = profile.storyText;
  document.getElementById('adminRole').value = profile.role;
  document.getElementById('adminLocation').value = profile.location;
  adminProfileImageInput.value = profile.profileImage || '';
  pendingProfileImage = null;
  adminProfileFileInput.value = '';
  document.getElementById('adminEmail').value = profile.email;
  document.getElementById('adminPhone').value = profile.phone;
  document.getElementById('adminFacebook').value = profile.facebook;
  document.getElementById('adminLinkedin').value = profile.linkedin;
  document.getElementById('adminGithub').value = profile.github;
  document.getElementById('adminInstagram').value = profile.instagram;
}

function updateClock() {
  const now = new Date();
  liveClock.textContent = now.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
  year.textContent = now.getFullYear();
}

function readSelectedImage(file) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => resolve(reader.result);
    reader.onerror = () => reject(new Error('Could not read image file'));
    reader.readAsDataURL(file);
  });
}

function unlockAdmin() {
  const enteredPassword = adminPasswordInput.value.trim();
  if (enteredPassword === (localStorage.getItem('portfolio-admin-password') || 'nayem2026')) {
    document.getElementById('admin').classList.remove('hidden-panel');
    document.getElementById('adminPanelBody').classList.remove('hidden-body');
    adminAccessPanel.classList.add('hidden');
    adminFormWrapper.classList.remove('hidden');
    adminLockStatus.textContent = '';
  } else {
    adminLockStatus.textContent = 'Wrong password. Please try again.';
  }
}

function showAdminPanel() {
  document.getElementById('admin').classList.remove('hidden-panel');
  document.getElementById('adminPanelBody').classList.remove('hidden-body');
  adminAccessPanel.classList.remove('hidden');
  adminFormWrapper.classList.add('hidden');
  adminLockStatus.textContent = '';
  adminPasswordInput.value = '';
}

function toggleAdminBody() {
  const panel = document.getElementById('adminPanelBody');
  panel.classList.toggle('hidden-body');
}

languageToggle.addEventListener('click', () => {
  const nextLang = currentLang === 'en' ? 'bn' : currentLang === 'bn' ? 'tr' : 'en';
  setLanguage(nextLang);
});

themeToggle.addEventListener('click', () => {
  const nextTheme = currentTheme === 'dark' ? 'light' : 'dark';
  setTheme(nextTheme);
});

notificationBell.addEventListener('click', () => {
  notifPanel.classList.toggle('open');
});

adminNavLink.addEventListener('click', (event) => {
  event.preventDefault();
  showAdminPanel();
  document.getElementById('admin').scrollIntoView({ behavior: 'smooth', block: 'start' });
});

let adminTapCount = 0;
let adminTapTimer = null;

window.addEventListener('keydown', (event) => {
  if (event.key === 'Escape') {
    document.getElementById('admin').classList.add('hidden-panel');
    document.getElementById('adminPanelBody').classList.add('hidden-body');
  }
});

document.addEventListener('click', (event) => {
  const target = event.target;
  if (!(target instanceof HTMLElement)) return;

  if (target.closest('.admin-link')) {
    event.preventDefault();
    adminTapCount += 1;
    if (adminTapTimer) clearTimeout(adminTapTimer);
    adminTapTimer = setTimeout(() => {
      adminTapCount = 0;
    }, 400);

    if (adminTapCount >= 2) {
      showAdminPanel();
      document.getElementById('admin').scrollIntoView({ behavior: 'smooth', block: 'start' });
      adminTapCount = 0;
    }
    return;
  }

  if (!notifPanel.contains(event.target) && !notificationBell.contains(event.target)) {
    notifPanel.classList.remove('open');
  }
});


document.addEventListener('click', (event) => {
  if (!notifPanel.contains(event.target) && !notificationBell.contains(event.target)) {
    notifPanel.classList.remove('open');
  }
});

helpForm.addEventListener('submit', (event) => {
  event.preventDefault();
  const data = new FormData(helpForm);
  const name = data.get('name')?.toString().trim() || 'Guest';
  const subject = data.get('subject')?.toString().trim() || 'Portfolio support';
  const message = data.get('message')?.toString().trim() || 'No additional message provided.';
  const mailtoLink = `mailto:hello@nayem.dev?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(`Name: ${name}\n\nMessage: ${message}`)}`;
  window.location.href = mailtoLink;
  helpStatus.textContent = 'Thanks! Your message has been prepared for delivery.';
  helpForm.reset();
});

adminProfileFileInput.addEventListener('change', async (event) => {
  const file = event.target.files?.[0];
  if (!file) return;

  try {
    const dataUrl = await readSelectedImage(file);
    pendingProfileImage = dataUrl;
    adminProfileImageInput.value = dataUrl;
    document.getElementById('heroImage').src = dataUrl;
    adminStatus.textContent = 'Image ready to upload.';
  } catch (error) {
    adminStatus.textContent = 'Could not read the selected image.';
    console.error(error);
  }
});

adminUnlockBtn.addEventListener('click', unlockAdmin);
document.querySelector('.admin-toggle-btn').addEventListener('click', () => {
  if (document.getElementById('admin').classList.contains('hidden-panel')) {
    document.getElementById('admin').classList.remove('hidden-panel');
  }
  toggleAdminBody();
});
adminPasswordInput.addEventListener('keydown', (event) => {
  if (event.key === 'Enter') {
    event.preventDefault();
    unlockAdmin();
  }
});

adminForm.addEventListener('submit', async (event) => {
  event.preventDefault();
  const data = new FormData(adminForm);
  const profileImageValue = pendingProfileImage || data.get('profileImage')?.toString().trim() || defaultProfile.profileImage;
  const profile = {
    displayName: data.get('displayName')?.toString().trim() || defaultProfile.displayName,
    heroTitle: data.get('heroTitle')?.toString().trim() || defaultProfile.heroTitle,
    heroText: data.get('heroText')?.toString().trim() || defaultProfile.heroText,
    storyText: data.get('storyText')?.toString().trim() || defaultProfile.storyText,
    role: data.get('role')?.toString().trim() || defaultProfile.role,
    location: data.get('location')?.toString().trim() || defaultProfile.location,
    profileImage: profileImageValue,
    email: data.get('email')?.toString().trim() || defaultProfile.email,
    phone: data.get('phone')?.toString().trim() || defaultProfile.phone,
    facebook: data.get('facebook')?.toString().trim() || defaultProfile.facebook,
    linkedin: data.get('linkedin')?.toString().trim() || defaultProfile.linkedin,
    github: data.get('github')?.toString().trim() || defaultProfile.github,
    instagram: data.get('instagram')?.toString().trim() || defaultProfile.instagram,
  };

  try {
    await saveProfileToServer(profile);
    applyProfile(profile);
    adminStatus.textContent = 'Your profile has been updated.';
  } catch (error) {
    adminStatus.textContent = 'Could not save changes. Please try again.';
  }
});

(async () => {
  const profile = await loadProfileFromServer();
  applyProfile(profile);
  populateAdminForm(profile);
})();

setTheme(currentTheme);
setLanguage(currentLang);
updateClock();
setInterval(updateClock, 1000);
